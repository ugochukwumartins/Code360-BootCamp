<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Code360App.listOfPages.GetAllQuestions">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="lists" HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label  Text="{Binding Option1}" TextColor="Black"/>
                                <Label  Text="{Binding Option2}" TextColor="Black"/>
                                <Label  Text="{Binding Option3}" TextColor="Black"/>
                                <Label  Text="{Binding Option4}" TextColor="Black"/>
                                <Label  Text="{Binding Answer}" TextColor="Black"/>
                            </StackLayout>

                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>